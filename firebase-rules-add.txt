// DIESE RULES MÜSSEN HINZUGEFÜGT WERDEN:

// 1. Favoriten Subcollection (analog zu einkaufswagen)
match /users/{parent}/favorites/{document} {
  allow create: if request.auth != null;
  allow read: if true;
  allow write: if request.auth != null;
  allow delete: if request.auth != null;
}

match /{path=**}/favorites/{document} {
  allow read: if true;
}

// 2. Achievements Collection
match /achievements/{document} {
  allow read: if true;
  allow write: if false;
}

// WICHTIG: Diese Rules zwischen den bestehenden einfügen!

