# Core OCR pipeline (used by validate.py CLI + dashboard.py UI)
google-genai>=0.3.0
pydantic>=2.6
python-dotenv>=1.0

# Document AI engine (alternative to Gemini — deterministic OCR)
google-cloud-documentai>=2.30.0

# Cloud Vision OCR (used by hybrid CV+Gemini engine — deterministic text extraction)
google-cloud-vision>=3.7.0

# Web dashboard (dashboard.py only — CLI usage doesn't need these)
streamlit>=1.50
pillow>=10.0

# Image preprocessing (auto-deskew, perspective correction, EXIF rotate)
opencv-python-headless>=4.10
numpy>=1.26

# Production Monitor (pages/01_Production_Monitor.py only):
# reads receipts collection live from Firestore + bon images from Storage
# + reprocesses via PubSub. Uses Application Default Credentials —
# run `gcloud auth application-default login` once.
firebase-admin>=6.5
google-cloud-pubsub>=2.21
pandas>=2.2
